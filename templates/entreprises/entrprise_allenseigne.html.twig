
        <div class="modal-header badge badge-secondary">
            <h5 class="modal-title">Voici la liste des enseignes de l'Entreprise</h4>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span></button>
        </div>
        <div class="modal-body">
            {% if enseignes != null  %}
                <table  class="table table-striped text-center">
                <thead>
                    <tr>
                        <th>Nom de l'Enseigne</th>
                        <th>Code de l'Enseigne </th>
                    </tr>
                </thead>
                <tbody>
                    {% for enseigne in enseignes %}
                    <tr>
                        <td>{{ enseigne.getNomEnseigne}}</td>
                        <td>{{ enseigne.getCodeEnseigne}}</td>
                    </tr>
                    {% endfor %}
                    </tfoot>
                </table>
                {% else %}
                <div class="alert alert-warning text-center" role="alert"> Aucun enseigne n'est créer par cette entreprise </div>
                {% endif %}
        </div>
        <div class="modal-footer card-footer text-right py-0">
            <button type="button" class="btn btn-default" data-dismiss="modal">fermer</button>
        </div>